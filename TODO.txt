  - Lazy Loading
  - Credit doesn't work
  - Subscriptions: add subscription and destroy
  - Use interfaces
- Use string for url routes, instead of dates (diario, mensual)
- Login with google or facebook
- add gasto desde pantalla de mes
- url con caracteres especiales, pincha. Por ejemplo con caracter: (
- poder buscar en las tablas
- en pc dejar el menu siempre visible
	- traducciones I18N
- preguntar antes de hacer el logout
- Interceptor de errores
- hay modelos que se comparten entre webapi y frontend, por ejemplo el de usuarios -> unirlos
- prevenir sql inyection en los forms
- capcha en registracion
- unit testing frontend
- en los post validar largos de los campos
- usar constantes para los status code del frontend
- el mensaje de error de cada pantalla que tenga un boton de retry
- Conceptos que no sumen ni resten: y que puedan ser cargados en multiples monedas
	- Github CD/CI (backend)
	- Login remove key sensitive in email
	- Update to angular 13
	- Forgot password
- Enviar email al finallizar mes con summary
	- Mensual: remove items with ammount 0
- There are deprecated warnings when running npm install
	- Create APK app
	- Loading animation when app is starting
	- Logo in login
	- Show password icon
	- Flags in language selection
- Reutilize code (services) for mobile and web projects
- Web project: if mobile browser is detected, force to download apk => https://play.google.com/store/apps/details?id=com.moneyguard.mobile
	- Use npm ci instead of npm i
	- Registration: set default language
	- Create npm control for password (use it in login & registration)
	- CI in gitub: create trigger for each component/module
	- Azure service bus to send emails
- Backend: use Microsoft Identity with Azure Table Storage
- Use redux to handle app status
- When release is in progress, show a message
	- First login: send user to load concepts
	- Black mode => registration => born date => the component does not show the icons < >
- Github actins: crear apk automatically
	- Concepts: dont reload the list after adding or modifying an item
	- Github actions: fix all actions just like sendgmail
	- Android: use generic error handler and save those errors in the internal log file => https://pkief.medium.com/global-error-handling-in-angular-ea395ce174b1
	- Android: Send internal log files to server
- Push notifications
- Get DB backup from azure
- Performance metric for backend
- Performance metric for cosmosdb
- Use USD as main currency for comparing
- Detect autatically if there is an update in the apk, and redirect to google play
	- Add graphs 
	- Create ionic project
	- Daily screen: toogle with 'more used concepts'
- UI: add transitions effects between screens
	- Export to excel file
- Apple compatibility
	- Forgot password => unblock user if blocked
	- Filter to hide items with amount 0
	- Use native date pickers
- Login => don`t create component if authentication is correct. Put condition on routing	
	- After install new version, the default currency is '$'. You have to logout and login to fix it
